{
    "$id": "properties-directory/scalar/coercive-voltage",
    "$schema": "http://json-schema.org/draft-07/schema#",
    "title": "Coercive voltage property schema",
    "description": "Bias at which the response of a hysteresis loop crosses its offset on one branch",
    "type": "object",
    "allOf": [
        {
            "$ref": "../../core/primitive/scalar.json"
        }
    ],
    "properties": {
        "name": {
            "enum": [
                "coercive_voltage"
            ]
        },
        "units": {
            "$ref": "../../definitions/units.json#/voltage"
        },
        "field": {
            "description": "whether the loop was read with the bias applied (on, in-field) or after each pulse (off, out-of-field)",
            "enum": [
                "on",
                "off"
            ]
        },
        "branch": {
            "description": "the branch of the loop the value is read from",
            "enum": [
                "rising",
                "falling"
            ]
        }
    },
    "required": [
        "name",
        "units",
        "field",
        "branch"
    ]
}
