{
    "$id": "properties-directory/scalar/loop-height",
    "$schema": "http://json-schema.org/draft-07/schema#",
    "title": "Loop height property schema",
    "description": "Height of a hysteresis loop: the difference between the falling and rising remanent responses",
    "type": "object",
    "allOf": [
        {
            "$ref": "../../core/primitive/scalar.json"
        }
    ],
    "properties": {
        "name": {
            "enum": [
                "loop_height"
            ]
        },
        "units": {
            "$ref": "../../definitions/units.json#/length"
        },
        "field": {
            "description": "whether the loop was read with the bias applied (on, in-field) or after each pulse (off, out-of-field)",
            "enum": [
                "on",
                "off"
            ]
        }
    },
    "required": [
        "name",
        "units",
        "field"
    ]
}
