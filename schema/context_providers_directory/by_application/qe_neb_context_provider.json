{
    "$id": "context-providers-directory/by-application/qe-neb-context-provider",
    "$schema": "http://json-schema.org/draft-07/schema#",
    "title": "QE NEB Context Provider Schema",
    "description": "Schema for QENEBContextProvider that generates context data for Quantum ESPRESSO pw.x NEB input. Extends the pw.x context with image-specific atomic positions.",
    "type": "object",
    "properties": {
        "IBRAV": {
            "$ref": "../../apse/file/applications/espresso/7.2/pw.x/system.json#/properties/ibrav"
        },
        "RESTART_MODE": {
            "type": "string",
            "enum": [
                "restart",
                "from_scratch"
            ],
            "description": "Restart mode for the calculation"
        },
        "ATOMIC_SPECIES": {
            "$ref": "../../apse/file/applications/espresso/7.2/pw.x/atomic_species.json#/properties/values"
        },
        "ATOMIC_SPECIES_WITH_LABELS": {
            "type": "string",
            "description": "Formatted text block for ATOMIC_SPECIES card with element labels (e.g., Fe1, C1). Format: 'Xn Mass_X PseudoPot_X' per line"
        },
        "NAT": {
            "$ref": "../../apse/file/applications/espresso/7.2/pw.x/system.json#/properties/nat"
        },
        "NTYP": {
            "$ref": "../../apse/file/applications/espresso/7.2/pw.x/system.json#/properties/ntyp"
        },
        "NTYP_WITH_LABELS": {
            "type": "integer",
            "description": "Number of different atomic species including labels",
            "minimum": 1
        },
        "ATOMIC_POSITIONS": {
            "$ref": "../../apse/file/applications/espresso/7.2/pw.x/atomic_positions.json#/properties/values"
        },
        "ATOMIC_POSITIONS_WITHOUT_CONSTRAINTS": {
            "type": "string",
            "description": "Formatted text block for ATOMIC_POSITIONS card WITHOUT constraints for a given image. Format: 'X x y z' per line"
        },
        "CELL_PARAMETERS": {
            "$ref": "../../apse/file/applications/espresso/7.2/pw.x/cell_parameters.json#/properties/values"
        },
        "FIRST_IMAGE": {
            "type": "string",
            "description": "Atomic positions block (ATOMIC_POSITIONS) for the first NEB image."
        },
        "LAST_IMAGE": {
            "type": "string",
            "description": "Atomic positions block (ATOMIC_POSITIONS) for the last NEB image."
        },
        "INTERMEDIATE_IMAGES": {
            "type": "array",
            "description": "Atomic positions blocks (ATOMIC_POSITIONS) for all intermediate NEB images.",
            "items": {
                "type": "string"
            }
        }
    },
    "required": [
        "IBRAV",
        "RESTART_MODE",
        "ATOMIC_SPECIES",
        "ATOMIC_SPECIES_WITH_LABELS",
        "NAT",
        "NTYP",
        "NTYP_WITH_LABELS",
        "CELL_PARAMETERS",
        "FIRST_IMAGE",
        "LAST_IMAGE",
        "INTERMEDIATE_IMAGES"
    ]
}
