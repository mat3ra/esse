{
    "$id": "materials-category/base/builder",
    "$schema": "http://json-schema.org/draft-07/schema#",
    "title": "Base Builder Schema",
    "type": "object",
    "description": "Base schema for all material builders",
    "properties": {
        "build_parameters": {
            "description": "Parameters used for the build process, specific to each builder implementation",
            "$ref": "builder_parameters.json"
        },
        "selector_parameters": {
            "description": "Parameters used to select from multiple generated items",
            "$ref": "selector_parameters.json"
        },
        "configuration_type": {
            "description": "The configuration type used by this builder",
            "type": "object"
        },
        "generated_item_type": {
            "description": "The type of items generated by this builder",
            "type": "object"
        },
        "selector_type": {
            "description": "The type of selector parameters",
            "type": "object",
            "properties": {}
        },
        "post_process_type": {
            "description": "The type of post-processing parameters",
            "type": "object",
            "properties": {}
        }
    },
    "required": [
        "build_parameters"
    ]
}
