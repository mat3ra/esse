{
    "$id": "material/operations/combinations/merge",
    "$schema": "http://json-schema.org/draft-07/schema#",
    "title": "Merge Schema",
    "description": "A component of a stack, which can be a crystal or a vacuum",
    "properties": {
        "merge_components": {
            "type": "array",
            "items": {
                "anyOf": [
                    {
                        "$ref": "../../primitive/three-dimensional/crystal.json"
                    },
                    {
                        "$ref": "../../primitive/two-dimensional/vacuum.json"
                    },
                    {
                        "$ref": "../../primitive/three-dimensional/void.json"
                    }
                ]
            }
        },
        "method": {
            "type": "string",
            "description": "Method to merge components: add, subtract (when merging with void), replace (overwrite upon collision), or yield (keep previous upon collision)",
            "enum": [
                "add",
                "replace",
                "yield"
            ]
        }
    }
}
