{
    "$id": "core/reusable/quantity/temperature",
    "$schema": "http://json-schema.org/draft-07/schema#",
    "title": "temperature quantity schema",
    "description": "A scalar with temperature units. Follows the core/reusable/energy pattern (a scalar plus a units enum) without requiring a property name, so it can be embedded as a field of any schema.",
    "type": "object",
    "allOf": [
        {
            "$ref": "../../primitive/scalar.json"
        }
    ],
    "properties": {
        "units": {
            "$ref": "../../../definitions/units.json#/temperature"
        }
    },
    "required": [
        "value",
        "units"
    ]
}
