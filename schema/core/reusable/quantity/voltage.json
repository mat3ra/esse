{
    "$id": "core/reusable/quantity/voltage",
    "$schema": "http://json-schema.org/draft-07/schema#",
    "title": "voltage quantity schema",
    "description": "A scalar with voltage units, e.g. a sample bias or drive amplitude. Follows the core/reusable/energy pattern (a scalar plus a units enum) without requiring a property name, so it can be embedded as a field of any schema.",
    "type": "object",
    "allOf": [
        {
            "$ref": "../../primitive/scalar.json"
        }
    ],
    "properties": {
        "units": {
            "$ref": "../../../definitions/units.json#/voltage"
        }
    },
    "required": [
        "value",
        "units"
    ]
}
