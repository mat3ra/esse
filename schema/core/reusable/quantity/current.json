{
    "$id": "core/reusable/quantity/current",
    "$schema": "http://json-schema.org/draft-07/schema#",
    "title": "current quantity schema",
    "description": "A scalar with current units, e.g. a tunneling current setpoint. Follows the core/reusable/energy pattern (a scalar plus a units enum) without requiring a property name, so it can be embedded as a field of any schema.",
    "type": "object",
    "allOf": [
        {
            "$ref": "../../primitive/scalar.json"
        }
    ],
    "properties": {
        "units": {
            "$ref": "../../../definitions/units.json#/current"
        }
    },
    "required": [
        "value",
        "units"
    ]
}
