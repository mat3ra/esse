# generated by datamodel-codegen:
#   filename:  sample/set.json
#   version:   0.28.5

from __future__ import annotations

from enum import Enum
from typing import List, Optional

from pydantic import BaseModel, Field


class Wafer(BaseModel):
    physicalId: str
    """
    The identifier written on the wafer's case
    """
    id: Optional[str] = Field(None, alias="_id")
    """
    The wafer's record
    """


class Units(Enum):
    km = "km"
    m = "m"
    cm = "cm"
    mm = "mm"
    um = "um"
    nm = "nm"
    angstrom = "angstrom"
    a_u_ = "a.u."
    bohr = "bohr"
    pm = "pm"


class Origin(BaseModel):
    coordinates: List[float] = Field(..., max_length=3, min_length=2)
    """
    The reading's axes, in the instrument's order
    """
    units: Units


class SampleSetSchema(BaseModel):
    wafer: Wafer
    """
    The wafer the placement is of
    """
    origin: Optional[Origin] = None
    """
    The instrument's position reading at the placement's starting point
    """
