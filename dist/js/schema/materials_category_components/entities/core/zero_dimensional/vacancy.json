{
    "$id": "materials-category-components/entities/core/zero-dimensional/vacancy",
    "$schema": "http://json-schema.org/draft-07/schema#",
    "title": "Vacancy Schema",
    "description": "A chemical element that can be placed at a crystal site",
    "type": "object",
    "required": [
        "chemical_element"
    ],
    "properties": {
        "chemical_element": {
            "default": "Vac",
            "description": "Extra elements, used for convenience purposed",
            "enum": [
                "X",
                "Vac"
            ]
        }
    }
}